import { AppIfAdminDirective } from './app-if-admin.directive';

describe('AppIfAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new AppIfAdminDirective();
    expect(directive).toBeTruthy();
  });
});
